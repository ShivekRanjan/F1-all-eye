# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Virtual environments
.venv/
venv/
env/

# FastF1 cache (large, regenerable — never commit)
data/cache/
*.ff1pkl

# Data artifacts
data/raw/
# Per-race intermediates are large and regenerable — keep them out.
data/processed/by_race/
data/processed/by_race_status/
data/processed/by_race_pitlaps/
data/processed/by_race_sprints/
*.parquet
!tests/fixtures/*.parquet
# ...but DO ship the small concatenated datasets the app needs to run (~1.3 MB),
# so the deployed container starts instantly without hitting FastF1.
!data/processed/dry_laps.parquet
!data/processed/track_status.parquet
!data/processed/race_laps.parquet
!data/processed/results.parquet
!data/processed/sprint_points.parquet
!data/processed/driver_info.parquet
!data/processed/track_layouts.parquet

# Notebooks
.ipynb_checkpoints/

# Models
*.pkl
*.pt
*.joblib
models_out/
# Seed replicates of the NLU transformer. Only the shipped artifact
# (nlu_intent_slot.npz) is tracked; the replicates exist to measure run-to-run
# variance and are regenerable with `nlu_train.py --seed N --out ...`. Three
# more copies of the same 1.7 MB archive is not history worth keeping.
data/processed/nlu_seed*.npz

# Tooling
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Local Claude Code settings (machine-specific)
.claude/settings.local.json
.claude/launch.json

# Node / frontend
node_modules/

# dev server logs (uvicorn.log, frontend/vite.log, ...)
*.log

# personal scratch notes (kept locally, not in the repo)
to run frend and bkend.txt
